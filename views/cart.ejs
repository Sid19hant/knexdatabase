<!DOCTYPE html>
<html lang="en">
<head>
    <!-- Head content -->
</head>
<body>
    <h1>Cart</h1>
    
    <!-- Display Cart Items -->
    <ul>
        <% cartItems.forEach(item => { %>
            <li><%= item.name %> - $<%= item.price %> (Quantity: <%= item.quantity %>)</li>
        <% }); %>
    </ul>
    
    <!-- Add more cart-related content or functionality as needed -->
</body>
</html>
